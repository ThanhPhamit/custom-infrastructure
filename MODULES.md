# Custom Terraform Module Catalog
> Auto-generated by /iac-implement. Refresh: delete this file and re-run, or it regenerates when a module changes.
> Required inputs = variables without a `default`. Key outputs truncated — see each module's outputs file.

| Module | Purpose | Required inputs | Key outputs |
|--------|---------|-----------------|-------------|
| `acm` | ACM public certificate (+ optional us-east-1 for CloudFront) | (all defaulted) | certificate_arn, virginia_certificate_arn |
| `alb` | Application Load Balancer + SG + listeners (blue/green) | app_name, acm_certificate_arn, vpc_id, restricted_source_ips, subnet_ids | alb_arn, domain, alb_security_group_id |
| `alert_email` | SNS + Lambda email alerting | app_name, sender_email, recipient_emails | sns_topic_arn, lambda_function_arn |
| `aws_oidc_with_github_actions` | GitHub Actions OIDC provider + IAM role | app_name, github_org, github_repositories, passrole_target_role_arns | oidc_provider_arn, iam_role_arn |
| `bastion_host` | **Bastion-only** EC2 (SG = 22 only; instance profile SSM-only) + start/stop scheduler | app_name, ami_id, vpc_id, subnet_id | bastion_instance_id, bastion_public_ip, ssh_config |
| `chatbot_slack` | AWS Chatbot → Slack channel | app_name, slack_workspace_id, slack_channel_id, slack_channel_name | chatbot_sns_topic_arn |
| `client_VPN_endpoints` | AWS Client VPN endpoint + associations | app_name, vpn_domain, vpc_id, subnet_ids, private_subnet_cidrs, enable_vpn_associations | — |
| `cloudfront` | CloudFront distribution in front of an ALB | app_name, alb_domain_name | cloudfront_domain_name, cloudfront_distribution_id |
| `cloudwatch_alarm_ecs` | CloudWatch alarms for ECS + log error patterns | cw_alarm_ecs_log_error_patterns, ecs_cloudwatch_log_group_name, lb_id | — |
| `cloudwatch_alarm_elasticache_server_based` | CloudWatch alarms for ElastiCache nodes | app_name, aws_region, cache_nodes, chatbot_notice_sns_topic_arn, chatbot_alert_sns_topic_arn | — |
| `cloudwatch_alarm_rds_instance` | CloudWatch alarms for an RDS instance | (all defaulted) | — |
| `codedeploy` | CodeDeploy app + deployment group (ECS blue/green) | task_definition_arn, container_name, container_port | s3_bucket_name, create_deployment_script |
| `ec2_instance` | **General EC2 instance** + SG (caller ingress_rules: CIDR/source-SG) + optional EIP + optional EBS data volume; IMDSv2; encrypted EBS | app_name, ami_id, instance_type, vpc_id, subnet_id | instance_id, private_ip, public_ip, security_group_id, data_volume_id |
| `ec2_scheduler` | Start/stop EC2 instances on a cron via EventBridge Scheduler (no Lambda) + scoped IAM role | app_name, instance_arns | start_schedule_arn, stop_schedule_arn, scheduler_role_arn |
| `ecr_private_registry` | Private ECR repository | repository_name | repository_url, repository_arn |
| `ecs` | ECS Fargate service + task def + target groups | region, container_names, container_port, http_prod_listener_arn, http_test_listener_arn, alb_security_group_id, ecs_security_group_id, repository_arn | service_name, task_definition_arn, ecs_task_role_arn |
| `ecs_cluster` | ECS cluster | (all defaulted) | cluster_name, cluster_arn |
| `ecs_server(sample)` | Sample full ECS service (reference) | (many app-specific) | service_name, app_secrets_arn |
| `elasticache_server_based` | ElastiCache Redis (node-based) | number_cache_clusters | primary_endpoint_address, security_group_id |
| `elasticache_serverless` | ElastiCache Serverless | (all defaulted) | primary_endpoint, security_group_id |
| `iam_role` | Generic IAM role (assume via `identifier`) + attached policy ARNs | name, policy_arns_map, identifier | iam_role_arn, iam_role_name |
| `internal_acm` | Private CA + internal certificate | domain, app_dns_zone | certificate_arn, ca_certificate_pem |
| `lightsail` | Lightsail instance + static IP + SSH key + firewall + IAM app user (keys → Secrets Manager) | app_name | instance_name, public_ip, private_ip, static_ip, iam_credentials_secret_arn |
| `lightsail_dns` | Route 53 records (A) → Lightsail static IP | dns_zone | dns_records, zone_id |
| `network` | VPC + public/private/database/elasticache/intra subnets + IGW + (optional) NAT + endpoints | (all defaulted) | vpc_id, public_subnet_ids, private_subnet_ids, public_subnets, database_subnet_group_name |
| `nlb` | Network Load Balancer + SG + TLS listeners + fixed EIPs | app_name, vpc_id, restricted_source_ips, subnet_ids | nlb_arn, dns_name, elastic_ip_addresses |
| `rds` | RDS/Aurora instance + subnet group + SG + password secret | app_name, db_name, vpc_id, availability_zone | db_endpoint, password_secret_arn, security_group_id |
| `rds_rotation_rollout` | Lambda to roll out RDS password rotation | app_name, region, rds_password_secret_arn | lambda_function_arn |
| `rds_secret_rotation` | Secrets Manager rotation Lambda for RDS | app_name, region, secret_arn, vpc_id, subnet_ids, rds_security_group_id | rotation_lambda_arn |
| `s3_api_assets` | Private S3 bucket for API assets | (all defaulted) | bucket_name, bucket_id |
| `s3_backend_storage` | Private S3 (SSE, versioning, CORS, public-access-block) + optional access policy | app_name | bucket_id, bucket_arn, s3_access_policy_arn, s3_access_policy_json |
| `s3_frontend` | S3 static site + CloudFront + optional basic-auth | app_name | bucket_name, cloudfront_domain_name, website_urls |
| `ses` | SES domain identity (optional) + SMTP user (`create_smtp_user`); SMTP creds in Secrets Manager | email_identities | smtp_host, smtp_username_secret_arn, smtp_password_secret_arn, domain_verification_token |
| `vpc-endpoints-network` | Interface/gateway VPC endpoints | vpc_id, subnet_ids, allowed_security_group_ids, route_table_ids | — |
| `waf_monitoring` | WAF logging + dashboards + Athena | app_name, scope, sns_topic_arn, web_acl_arn, active_rules | dashboard_name, athena_table_name |
| `waf_standard` | Standard WAF web ACL (managed rules + rate limit) | app_name, scope | web_acl_arn, ip_set_arn |
