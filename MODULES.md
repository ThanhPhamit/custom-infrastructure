# Custom Terraform Module Catalog
> Auto-generated by /iac-implement. Refresh: delete this file and re-run, or it regenerates when a module changes.

| Module | Purpose | Required inputs | Key outputs | Used in env (example) |
|--------|----------|-----------------|-------------|--------------------|
| acm | Public ACM cert with Route 53 DNS validation (regional + us-east-1) | domain, app_dns_zone | certificate_arn, certificate_id, virginia_certificate_arn | — |
| alb | Application Load Balancer with HTTPS listeners, SG, WAF assoc, optional mTLS trust store | app_name, acm_certificate_arn, vpc_id, restricted_source_ips, subnet_ids | alb_arn, domain, alb_security_group_id, lb_listener_http_prod_arn, trust_store_arn | — |
| alert_email | SNS topic + Lambda forwarder that emails alerts via SES | app_name, sender_email, recipient_emails | sns_topic_arn, sns_topic_name, lambda_function_arn, lambda_function_name | — |
| aurora_postgresql | Provisioned Aurora PostgreSQL cluster + instance(s) with RDS-managed master secret, IAM DB auth, `rds.force_ssl` cluster parameter group, PI + Enhanced Monitoring + log export, optional RDS event subscription + CPU alarms (AWS-managed `aws/rds` KMS by default) | app_name, vpc_id, subnet_ids, allowed_cidr_blocks | cluster_endpoint, cluster_reader_endpoint, cluster_resource_id, master_user_secret_arn, security_group_id | s2s-vpn (external repo) |
| aws_oidc_with_github_actions | GitHub Actions OIDC provider + assumable IAM role/policy (keyless CI) | app_name, github_org, github_repositories, passrole_target_role_arns | oidc_provider_arn, iam_role_arn, iam_role_name, iam_policy_arn | — |
| bastion_host | EC2 bastion with EIP, SSM-only instance profile, SG, optional start/stop scheduler | app_name, ami_id, vpc_id, subnet_id | bastion_instance_id, bastion_public_ip, bastion_security_group_id, ssm_start_session_command, ssh_config | — |
| chatbot_slack | AWS Chatbot Slack channel config + SNS topic + IAM role | app_name, slack_workspace_id, slack_channel_id, slack_channel_name | chatbot_sns_topic_arn | — |
| client_VPN_endpoints | AWS Client VPN endpoint with self-signed TLS certs, SG, auth/routes, CW logs | app_name, vpn_domain, vpc_id, subnet_ids, private_subnet_cidrs | (none) | — |
| cloudfront | CloudFront distribution fronting an ALB origin with cache/request policies + Route 53 | app_name, alb_domain_name | cloudfront_distribution_id, cloudfront_domain_name, custom_domain, cloudfront_function_arn, access_urls | — |
| cloudwatch_alarm_ecs | ECS CPU/memory/log-error alarms + composite alarm + app autoscaling policies | app_name, aws_region, cw_alarm_cluster_name, cw_alarm_service_name, chatbot_alert_sns_topic_arn | (none) | — |
| cloudwatch_alarm_elasticache_server_based | Per-node ElastiCache alarms (CPU, memory, hit ratio, evictions, replication lag) | app_name, aws_region, cache_nodes, chatbot_notice_sns_topic_arn, chatbot_alert_sns_topic_arn | cpu_utilization_critical_alarm_arn, memory_usage_critical_alarm_arn, cache_hit_ratio_alarm_arn, evictions_alarm_arn, replication_lag_alarm_arn | — |
| cloudwatch_alarm_rds_instance | RDS metric alarms (CPUUtilization, FreeableMemory, FreeStorageSpace) at notice + alert thresholds, routed to SNS | app_name, rds_db_instance_identifier, cw_alarm_rds_cpu_utilization_alert_threshold, chatbot_notice_sns_topic_arn, chatbot_alert_sns_topic_arn | (none) | — |
| codedeploy | CodeDeploy app + ECS blue/green deployment group with artifact S3 bucket | aws_region, app_name, ecs_cluster_name, ecs_service_name, task_definition_arn | s3_bucket_name, create_deployment_script | — |
| ec2_instance | General EC2 instance with optional EIP, extra encrypted EBS volume, SG, IMDSv2 | app_name, ami_id, instance_type, vpc_id, subnet_id | instance_id, private_ip, public_ip, security_group_id, data_volume_id | — |
| ec2_scheduler | EventBridge Scheduler start/stop schedules + scoped IAM role for tagged EC2 instances | app_name, instance_arns | start_schedule_arn, stop_schedule_arn, scheduler_role_arn, managed_instance_ids | — |
| ecr_private_registry | Private ECR repository with lifecycle policy | repository_name | repository_url, repository_arn | — |
| ecs | ECS Fargate service + task definition, blue/green target groups, SG, IAM, CW logs | app_name, region, container_names, container_port, vpc_id | service_name, task_definition_arn, ecs_task_role_arn, lb_target_group_blue_arn, lb_target_group_green_arn | — |
| ecs_cluster | Standalone ECS cluster | app_name | cluster_name, cluster_arn | — |
| ecs_server(sample) | Sample ECS service variant wired to app env vars + Secrets Manager (postgres/cache/GMO) | app_name, region, container_names, container_port, vpc_id | service_name, task_definition_arn, app_secrets_arn, app_runtime_secret_arn, ecs_task_role_arn | — |
| elasticache_server_based | ElastiCache replication group (node-based) + subnet group, SG, CW logs | app_name, vpc_id, number_cache_clusters | primary_endpoint_address, reader_endpoint_address, cache_nodes, security_group_id, log_group_arns | — |
| elasticache_serverless | ElastiCache Serverless cache with SG | app_name, vpc_id, subnet_ids | primary_endpoint, reader_endpoint, security_group_id | — |
| iam_role | Creates one IAM role with a service-principal trust (single `identifier`) and attaches managed-policy ARNs via `policy_arns_map`; no custom trust-policy doc or inline-policy inputs | name, policy_arns_map, identifier | iam_role_arn, iam_role_name | — |
| internal_acm | Private/self-signed internal ACM cert (CA + server cert) with local file outputs | domain, app_dns_zone | certificate_arn, ca_certificate_pem, server_certificate_pem, installation_command | — |
| lightsail | Lightsail instance with static IP, key pair, firewall, IAM user + secret-stored credentials | app_name | instance_name, public_ip, static_ip, iam_credentials_secret_arn, ssh_config | — |
| lightsail_dns | Route 53 records for a Lightsail/DNS zone | dns_zone | dns_records, record_names, zone_id | — |
| network | Full VPC stack: VPC + public/private/database/intra/elasticache subnets with per-type route tables and subnet groups; IGW toggle (create_igw) + NAT toggles (enable_nat_gateway, single_nat_gateway, one_nat_gateway_per_az); VGW route propagation via default_route_table_propagating_vgws; optional S3/DynamoDB/SecretsManager endpoints | (none — all defaulted) | vpc_id, private_subnet_ids, database_subnets, database_subnet_group, vpc_endpoint_secretsmanager_id | tokyo-staging |
| nlb | Network Load Balancer with TLS listeners, SG, fixed/elastic IPs, Route 53 | app_name, vpc_id, restricted_source_ips, subnet_ids | nlb_arn, dns_name, lb_listener_tls_prod_arn, fixed_ip_addresses, elastic_ip_addresses | — |
| rds | Single RDS instance (Postgres default) with generated password in Secrets Manager, SG, parameter group, optional replica/S3 integration, CW logs | app_name, db_name, vpc_id, availability_zone | db_endpoint, db_hostname, password_secret_arn, security_group_id, db_subnet_group_name | — |
| rds_rotation_rollout | Lambda + EventBridge that rolls out/triggers RDS secret rotation | app_name, region, rds_password_secret_arn | lambda_function_name, lambda_function_arn, eventbridge_rule_arn | — |
| rds_secret_rotation | Enables Secrets Manager rotation for an RDS secret via SAR rotation Lambda in-VPC, with SG + schedule | app_name, region, secret_arn, vpc_id, subnet_ids, rds_security_group_id | rotation_lambda_name, rotation_lambda_arn, rotation_enabled_secret_arn | — |
| route53_resolver_inbound | Route 53 Resolver INBOUND endpoint (one ENI per subnet, pinnable IPs) so on-prem can resolve AWS private DNS over VPN/DX; DNS-53 SG restricted to on-prem CIDRs; optional Resolver query logging to CloudWatch | app_name, vpc_id, subnet_ids, allowed_cidr_blocks, vpc_cidr | resolver_inbound_ip_addresses, resolver_endpoint_id, security_group_id | s2s-vpn (external repo) |
| route53_resolver_outbound | Route 53 Resolver OUTBOUND endpoint + FORWARD rule + VPC association (mirror of route53_resolver_inbound — full hybrid DNS); egress-only DNS SG; pinnable ENI IPs for on-prem firewall allow-listing | app_name, vpc_id, subnet_ids, forward_domain, target_dns_ips | resolver_outbound_ip_addresses, forward_rule_id, security_group_id | s2s-vpn (external repo) |
| s3_api_assets | S3 bucket for API assets with CORS + CloudFront OAI bucket policy | app_name, api_domain, frontend_agent_domain, frontend_jobseeker_domain, origin_access_identity_iam_arn | bucket_name, bucket_id, bucket_regional_domain_name | — |
| s3_backend_storage | Private S3 bucket (versioning, SSE, lifecycle, public-access-block) + IAM access policy | app_name | bucket_id, bucket_arn, s3_access_policy_arn, s3_access_policy_json, bucket_regional_domain_name | — |
| s3_frontend | S3 static-site bucket + CloudFront (OAC) + Route 53, optional basic-auth secret | app_name | bucket_name, cloudfront_distribution_id, cloudfront_domain_name, website_urls, basic_auth_secret_arn | — |
| ses | SES domain + email identities, sending IAM user, SMTP creds in Secrets Manager, templates | app_name, route_53_zone_id, email_identities | smtp_host, smtp_username, smtp_secret_arn, domain_identity_arn, email_templates | — |
| site_to_site_vpn | VGW + CGW + Site-to-Site VPN with pinned IPsec proposals (IKEv2/AES-256/SHA2-256/DH-20/DPD); static AND/OR BGP dynamic-routing connections count-gated for validate-first migration (enable_bgp/retain_static, distinct-IP BGP CGW — AWS dedups CGW by ip+asn); VGW route propagation; optional TunnelState alarms covering both connections | app_name, vpc_id, customer_gateway_ip, onprem_cidr | vpn_gateway_id, bgp_vpn_connection_id, bgp_tunnel1/2_address (+PSKs sensitive), bgp_tunnel1/2 vgw/cgw inside addresses, amazon_side_asn | s2s-vpn (external repo) |
| sts_credential_vending | Assume-only bootstrap IAM user (optional access key) → least-privilege access role (GetSecretValue on ONE secret + optional rds-db:connect with `dbuser:` colon ARN); trust lockable to aws:SourceVpce + optional sts:ExternalId — no long-lived DB creds on the client | app_name, db_secret_arn, aws_region, aws_account_id | access_role_arn, bootstrap_user_name, bootstrap_access_key_id + bootstrap_secret_access_key (sensitive) | s2s-vpn (external repo) |
| vpc-endpoints-network | Interface VPC endpoints (ECR dkr/api, Logs, Secrets Manager, Bedrock, optional SSM trio) + Gateway S3 endpoint, all behind one shared SG allowing 443 from allowed_security_group_ids and optional VPN CIDRs | app_name, vpc_id, subnet_ids, allowed_security_group_ids, route_table_ids | (none) | — |
| vpc_interface_endpoints | Generic PrivateLink interface endpoints for arbitrary service_names behind one shared 443 SG (CIDR + SG allow-lists) with per-service endpoint policies + private DNS — service-agnostic replacement where vpc-endpoints-network is too ECS-shaped (e.g. STS/Secrets Manager/SSM trio) | app_name, vpc_id, subnet_ids, service_names | endpoint_ids, security_group_id | s2s-vpn (external repo) |
| waf_monitoring | WAFv2 logging to S3 + Athena/Glue, CloudWatch dashboard + alarms, IP set | app_name, scope, sns_topic_arn, web_acl_arn, active_rules | dashboard_name, alarm_arns, athena_workgroup_name, athena_table_name, s3_bucket_name | — |
| waf_standard | WAFv2 web ACL (managed rules + rate limit + IP set) with CloudFront/ALB association | app_name, scope | web_acl_arn, web_acl_id, ip_set_arn, effective_rate_limit, active_rules | — |
